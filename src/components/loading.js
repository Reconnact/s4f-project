import React from "react";
import ReactLoading from "react-loading";
const Loading = () => (
  <div style={{marginLeft: "45%"}}>
    <ReactLoading type={"spinningBubbles"} color="#56aaff" height={"10%"} width={"10%"} />
  </div>
);

export default Loading;